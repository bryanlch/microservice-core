import { Controller, Post, Body } from '@nestjs/common';
import { Create{{pascalCase name}}UseCase } from '../../application/use-cases/create-{{dashCase name}}.use-case';

@Controller('{{dashCase name}}')
export class {{pascalCase name}}Controller {
  constructor(private readonly create{{pascalCase name}}UseCase: Create{{pascalCase name}}UseCase) {}

  @Post()
  async create(@Body() createDto: any) {
    return this.create{{pascalCase name}}UseCase.execute(createDto);
  }
}
